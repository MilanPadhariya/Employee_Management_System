import { ApproveleaveComponent } from './approveleave.component';

describe('ApproveleaveComponent', () => {
  let component: ApproveleaveComponent;

  beforeEach(() => {
    component = new ApproveleaveComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
