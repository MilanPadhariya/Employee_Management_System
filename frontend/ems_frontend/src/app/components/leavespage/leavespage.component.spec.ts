import { LeavespageComponent } from './leavespage.component';

describe('LeavespageComponent', () => {
  let component: LeavespageComponent = new LeavespageComponent();

  beforeEach(() => {
    component = new LeavespageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
