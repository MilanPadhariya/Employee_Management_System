import { ProjectspageComponent } from './projectspage.component';

describe('ProjectspageComponent', () => {
  let component: ProjectspageComponent = new ProjectspageComponent();

  beforeEach(() => {
    component = new ProjectspageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
