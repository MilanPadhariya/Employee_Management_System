import { AllprojectsComponent } from './allprojects.component';

describe('AllprojectsComponent', () => {
  let component: AllprojectsComponent;

  beforeEach(() => {
    component = new AllprojectsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
